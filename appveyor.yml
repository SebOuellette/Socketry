environment:
  matrix:
    - nodejs_version: "10"
platform:
  - x86
  - x64
matrix:
  fast_finish: true
install:
  - npm install
test_script:
  - node --version
  - npm --version
  - npm test
build: off